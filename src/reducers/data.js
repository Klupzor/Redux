function data(state, action) {
    switch (action.type) {
        case 'SEARCH_VIDEO':
            // action.payload.query
            return state
    
        default:
            return state
    }
}

export default data;